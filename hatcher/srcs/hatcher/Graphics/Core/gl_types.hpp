#pragma once

// Manual typedefs in order to not include GL/glew.h each time we need GLuint.
using GLint = int;
using GLuint = unsigned int;
using GLenum = unsigned int;
using GLubyte = unsigned char;
